#!/usr/bin/env bash
# Galyarder Framework RTK Wrapper
REAL_RTK="/home/linuxbrew/.linuxbrew/bin/rtk"
if [ -f "$REAL_RTK" ]; then
    "$REAL_RTK" "$@"
else
    # Fallback to whatever is in PATH
    rtk "$@"
fi
